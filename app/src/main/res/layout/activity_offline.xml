<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/offline_coordinator"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <RelativeLayout
        android:id="@+id/offline_relative_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent" >

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/offline_notification"
            android:textAlignment="center"
            android:text="@string/offline_notification"
            android:layout_alignParentTop="true"
            android:layout_alignParentStart="true"
            android:layout_marginTop="28dp"
            android:layout_alignParentEnd="true"
            android:textSize="28sp" />

        <TextView
            android:text="@string/receipts_status_is_zero_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingLeft="30dp"
            android:paddingRight="30dp"
            android:textAlignment="center"
            android:layout_below="@+id/offline_notification"
            android:layout_marginTop="13dp"
            android:id="@+id/receipts_status_text"
            android:layout_alignParentEnd="true"
            android:layout_alignParentStart="true" />

        <Button
            android:text="@string/go_online_prompt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_centerHorizontal="true"
            android:id="@+id/go_online_button"
            android:fontFamily="sans-serif-medium" />
    </RelativeLayout>

    <com.autocounting.autocounting.views.widgets.CameraFab
        android:id="@+id/camera_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:src="@drawable/ic_menu_camera"
        android:tint="@color/white"
        app:layout_anchor="@id/offline_relative_layout"
        app:layout_anchorGravity="bottom|right|end"
        app:backgroundTint="@color/colorPrimary"
        app:fabSize="normal"/>

</android.support.design.widget.CoordinatorLayout>